# Copy this file to lichess/.env, replace the value, then run chmod 600 on it.
# Create the token while signed in to the new bot account and grant only bot:play.
LICHESS_BOT_TOKEN=replace_with_the_bot_accounts_token
